{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "[ $CI ] || husky install",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "test": "lerna run test",
    "version": "lerna version --no-push"
  },
  "devDependencies": {
    "eslint": "^8.35.0",
    "husky": "^8.0.3",
    "jest": "^29.4.3",
    "lerna": "^6.5.1",
    "lint-staged": "^13.1.2",
    "typescript": "^4.9.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "**/*.{js,ts,tsx}": [
      "eslint"
    ],
    "**/*": [
      "prettier --check"
    ]
  }
}
